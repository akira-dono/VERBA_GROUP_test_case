# VERBA GROUP test case

## P.S
Before starting, you need to create a `.env` file and put `db_username`, `db_host`, `db_name`, `db_password`, `db_port` into it.

### Install

1. Download or Clone repo
2. ``` npm install ```
3. Start application ``` npm run start ```

